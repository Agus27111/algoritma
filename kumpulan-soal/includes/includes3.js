// Kategori Lanjutan (Advanced)
// Soal:

// Berikan array mixed = [1, "2", 3, "4", 5]. Gunakan metode includes() untuk memeriksa apakah array mengandung angka 2.
const mixed = [1, "2", 3, "4", 5]
console.log(mixed.includes(2))
// Berikan array nested = [[1, 2], [3, 4], [5, 6]]. Gunakan metode includes() untuk memeriksa apakah array mengandung [3, 4].
const nested = [[1, 2], [3, 4], [5, 6]]
console.log(nested.includes([3,4]))
// Berikan array emptyValues = [null, undefined, ""]. Gunakan metode includes() untuk memeriksa apakah array mengandung undefined.

// Berikan array booleans = [true, false, true]. Gunakan metode includes() untuk memeriksa apakah array mengandung false mulai dari indeks 2.


// Berikan array objects = [{ a: 1 }, { b: 2 }, { c: 3 }]. Gunakan metode includes() untuk memeriksa apakah array mengandung { b: 2 }.


// Berikan array sparse = [1, , 3]. Gunakan metode includes() untuk memeriksa apakah array mengandung undefined.


// Berikan array specialNumbers = [NaN, Infinity, -Infinity]. Gunakan metode includes() untuk memeriksa apakah array mengandung NaN.


// Berikan array strings = ["a", "b", "c"]. Gunakan metode includes() untuk memeriksa apakah array mengandung "A" (case-sensitive).

// Berikan array nums = [1, 2, 3, 4, 5]. Gunakan metode includes() untuk memeriksa apakah array mengandung angka 3 mulai dari indeks 3.

// Berikan array duplicates = [1, 2, 2, 3, 4]. Gunakan metode includes() untuk memeriksa apakah array mengandung angka 2 mulai dari indeks 3.
